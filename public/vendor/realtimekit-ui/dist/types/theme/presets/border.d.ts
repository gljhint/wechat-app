import { BorderRadius, BorderWidth } from '../../types/ui-config/design-tokens';
export declare const BORDER_WIDTHS: Record<BorderWidth, {
    none: number;
    sm: number;
    md: number;
    lg: number;
}>;
export declare const BORDER_RADII: Record<BorderRadius, {
    none: number;
    sm: number;
    md: number;
    lg: number;
}>;
